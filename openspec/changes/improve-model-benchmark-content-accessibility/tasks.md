## 1. Evidence And Approval

- [x] 1.1 Trace toolbox/settings entry through the Workbench content, service projection, actions, final UI, and every adjacent F-22 owner.
- [x] 1.2 Inspect the production empty-state accessibility tree and DOM at 1280×720 without reading credentials, storage, or invoking a provider.
- [x] 1.3 Activate a modality with pointer input and prove visual/default-prompt state changes while programmatic selected state remains absent.
- [x] 1.4 Record input label relationships, control geometry, headings/regions, current focus, and hard-coded language ownership.
- [x] 1.5 Separate outer WinBox, execution/live status, storage feedback, diagnostics, ranking, handoff, shared components, compact geometry, and theme policy.
- [ ] 1.6 Obtain user approval for selected-state, label/region, and F-22 Chinese/English behavior.

## 2. Failing Tests And Stateful Controls (Approval Required)

- [ ] 2.1 Add failing empty/synthetic-session tests for modality, comparison mode, history filter, active session, score, favorite, and reject state.
- [ ] 2.2 Add pointer, Tab, Enter/Space, and chosen group-arrow behavior tests with exact callback counts and focus assertions.
- [ ] 2.3 Implement one consistent native/equivalent group contract and programmatic state without changing existing values or service calls.
- [ ] 2.4 Preserve ranking, execution/live status, deletion, storage, handoff, and outer-window owner boundaries.

## 3. Failing Tests And Form Structure (Approval Required)

- [ ] 3.1 Add failing tests for persistent labels on history search, model/provider/target selectors, prompt, concurrency, and any existing selection instructions.
- [ ] 3.2 Add failing tests for labelled history, builder, and result regions/headings without changing WinBox dialog semantics.
- [ ] 3.3 Implement stable F-22 wrapper IDs/relationships and preserve TDesign/native values, placeholders, disabled state, and callbacks.
- [ ] 3.4 Prove no prompt, provider/model/session/result/error value is copied into a translation key or unsafe accessible name.

## 4. Localization And Preservation (Approval Required)

- [ ] 4.1 Inventory application-authored Workbench strings separately from user/provider/model/session/prompt/result/error data.
- [ ] 4.2 Add typed Chinese/English F-22 keys and consume them for builder, history, empty, confirmation, result framing, and safe application feedback.
- [ ] 4.3 Add initial and live language-switch tests for empty and synthetic-result states.
- [ ] 4.4 Assert provider/model/session titles, prompts, preview values, errors, export data, callback arguments, and analytics metadata remain byte-for-byte unchanged.

## 5. Verification

- [ ] 5.1 Run focused Workbench/service tests with exact files/tests/exits and zero external provider/storage calls in interface fixtures.
- [ ] 5.2 Run focused ESLint and Drawnix typecheck, then repository gates against the recorded baseline.
- [ ] 5.3 Run available browser/E2E keyboard, screen-reader, zh/en, empty/result/confirmation flows and classify missing browser binaries separately.
- [ ] 5.4 Capture matched before/after screenshots only after implementation; do not claim performance or visual improvement without required measurements.
- [ ] 5.5 Recheck lifecycle/storage/diagnostic/ranking/handoff/window changes for owner overlap before implementation.
- [x] 5.6 Attempt strict OpenSpec validation; the CLI is unavailable (exit 127), so complete manual structure/scenario/name/owner checks without claiming strict validation.
